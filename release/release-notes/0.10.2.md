# Vibes 0.10.2

Fixes status sync for very busy Git days.

- Vibes now handles larger daily activity updates without showing a request-size error.
- The app keeps sharing aggregate commits and line counts even when a day has too many commits to send individually.

<!--
Sparkle renders this file as the release notes in the update dialog.
Keep it short, user-facing, and plain. Do not include internal repo paths,
branch names, commit messages, or tokens. One file per version.
-->
